// Imports
import React, { useCallback } from 'react';
import Particles from 'react-particles';
import { loadFull } from 'tsparticles';

function ParticleHolder() {
    return (
        <div>
            <span>Particles go here.</span>
        </div>
    );
}

export default ParticleHolder;
